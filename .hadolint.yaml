ignored:
  - DL3008  # Pin apt versions — impractical for images tracking upstream Debian
  - DL3059  # Multiple consecutive RUNs — split intentionally for readability
  - DL4006  # SHELL pipefail — official PHP image pattern relies on ldd exiting non-zero, incompatible with pipefail
